const fs = require('fs'); const path = require('path'); const configDir = path.join(__dirname, 'config'); const configPath = path.join(configDir, 'translator.json'); console.log('Fixing translator plugin configuration...'); if (!fs.existsSync(configDir)) { fs.mkdirSync(configDir, { recursive: true }); console.log('Created config directory:', configDir); } const config = { apiKey: '1234', defaultTargetLanguage: 'en', translateIncoming: false, translateOutgoing: true, outgoingLanguages: ['es', 'fr'] }; fs.writeFileSync(configPath, JSON.stringify(config, null, 2), 'utf8'); console.log('Saved translator configuration to:', configPath); console.log('Configuration:', JSON.stringify(config, null, 2));
