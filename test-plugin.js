const path = require('path'); const fs = require('fs'); const pluginPath = path.join(__dirname, 'plugins', 'translator.js'); console.log('Testing if translator plugin can be loaded...'); console.log('Plugin path:', pluginPath); console.log('File exists:', fs.existsSync(pluginPath)); try { const plugin = require(pluginPath); console.log('Plugin loaded successfully!'); console.log('Plugin name:', plugin.name); console.log('Plugin version:', plugin.version); } catch (error) { console.error('Error loading plugin:', error.message); }
